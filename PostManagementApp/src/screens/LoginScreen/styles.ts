import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  inner: {
    padding: 24,
    flex: 1,
    justifyContent: 'center'
  },
  alignCenter: {
    alignItems: 'center',
    marginTop: 20
  },
  textCenter: {
    textAlign: 'center'
  }
});
